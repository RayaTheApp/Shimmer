// swift-tools-version: 5.8

import PackageDescription

let package = Package(
    name: "Shimmer",
    products: [
        .library(
            name: "Shimmer",
            targets: ["Shimmer"]),
    ],
    targets: [
        .target(
            name: "Shimmer"),
    ]
)
